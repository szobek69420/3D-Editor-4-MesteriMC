#version 330 core

in vec2 uv;
in vec3 pos;

out vec4 fragColour;

uniform int isSampled;
uniform sampler2D tex;
uniform vec3 colour;

void main()
{
	fragColour=vec4(colour,1);
	if(isSampled!=0)
		fragColour=texture(tex, uv);
}