#version 330 core
uniform vec3 info;//isXdir, based, leptek
uniform mat4 vp;
void main()
{
	if(info.x>0) gl_Position=vec4(info.y+gl_InstanceID*info.z,0,1000-gl_VertexID*2000,1);
	else gl_Position=vec4(1000-gl_VertexID*2000,0,info.y+gl_InstanceID*info.z,1);
	gl_Position=gl_Position*vp;
}
